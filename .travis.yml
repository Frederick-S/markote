language: python
python:
  - 3.4
  - 3.5
  - 3.6
install:
  - pip install pycodestyle codecov
  - python setup.py install
  - npm install
script:
  - npm run lint
  - make lint
  - make coverage
after_success:
  - codecov
